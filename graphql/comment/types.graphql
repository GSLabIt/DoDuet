union Commentable = Lyric | Cover | Album | Track

type Comment {
    id: ID!
    commentor_id: User! @belongsTo
    content: String!
    commentable_id: Commentable @morphTo
    commentable_type: String!
    created_at: DateTime!
    updated_at: DateTime!
    explicit: Explicit @morphOne
}
